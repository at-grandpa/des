FROM <%= @options.image %>

RUN apt-get -y update
RUN apt-get -y upgrade
<%- unless @options.packages.empty? -%>
RUN apt-get -y install <%= @options.packages.join(" ") %>
<%- end -%>

WORKDIR /root/<%= @options.container %>
